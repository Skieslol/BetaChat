import { GuildSidebar } from "../components/guilds/GuildSidebar";
import { Page } from "../styles/styles";

export const Guild = () => {
    return (
        <Page>
            <GuildSidebar />        
        </Page>
    );
}