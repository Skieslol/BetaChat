import { GuildSidebarStyle } from "../../styles/styles";

export const GuildSidebar = () => {
    return (
        <GuildSidebarStyle>
            <header>
                Guilds
            </header>
        </GuildSidebarStyle>
    );
}